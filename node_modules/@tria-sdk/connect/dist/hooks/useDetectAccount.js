"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDetectAccount = void 0;
const react_1 = require("react");
const constants_1 = require("../utils/constants");
function useDetectAccount({ authUrl }) {
    const [account, setAccount] = (0, react_1.useState)(null);
    (0, react_1.useEffect)(() => {
        const detect = (event) => {
            if (event.origin !== authUrl)
                return;
            let eventData;
            try {
                eventData = JSON.parse(event.data);
            }
            catch (err) {
                eventData = event.data;
            }
            if ((eventData === null || eventData === void 0 ? void 0 : eventData.type) == constants_1.eventTypes.detected) {
                setAccount(eventData === null || eventData === void 0 ? void 0 : eventData.data);
            }
        };
        window.addEventListener('message', detect);
        return () => {
            window.removeEventListener('message', detect);
        };
    }, []);
    return { account };
}
exports.useDetectAccount = useDetectAccount;
