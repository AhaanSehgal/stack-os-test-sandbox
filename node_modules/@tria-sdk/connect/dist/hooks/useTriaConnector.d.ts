/**
 *
 * @param event event
 * @param type event type waiting for - like "Login, Signup, Sign"
 * @returns Json parsed data - message from the event
 */
export declare const parseEventData: (event: any) => any;
export declare function useTriaConnector({ authUrl, walletUrl, }: {
    authUrl: string;
    walletUrl: string;
}): {
    globalData: Map<any, any>;
};
