export type Account = {
    triaName: string;
    evm: {
        address: string;
    };
};
export type useAccountResponse = {
    account: Account | null;
};
export declare function useAccount(): useAccountResponse;
