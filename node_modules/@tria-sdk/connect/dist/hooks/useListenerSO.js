"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useListenerSO = void 0;
const react_1 = require("react");
const constants_1 = require("../utils/constants");
function useListenerSO() {
    const [eventData, setEventData] = (0, react_1.useState)();
    (0, react_1.useEffect)(() => {
        const authResponse = (event) => {
            if (event.origin !== window.origin)
                return;
            let eventData;
            try {
                eventData = JSON.parse(event.data);
            }
            catch (err) { }
            if (eventData && (eventData === null || eventData === void 0 ? void 0 : eventData.type) == constants_1.eventTypes.passMessage)
                setEventData(eventData);
        };
        window.addEventListener('message', authResponse);
    }, []);
    return { eventData };
}
exports.useListenerSO = useListenerSO;
