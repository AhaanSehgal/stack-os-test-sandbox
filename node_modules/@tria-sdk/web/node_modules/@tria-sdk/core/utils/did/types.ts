export type DidDataResponse = {
  didToEvmAddress: DidDataEvmResponse;
  didToNonEvmAddresses: DidDataNonEvmResponse[];
};

type DidDataEvmResponse = {
  did: string;
  evmAddress: string;
};

type DidDataNonEvmResponse = {
  did: string;
  nonEvmAddress: string;
  chain: {
    id: string;
    chainName: string;
  };
};
