export const formatChainNames = (filterChainNames: string[]) => {
  let chainNames;
  for (let [index, chainName] of filterChainNames.entries()) {
    if (index == 0) chainNames = `${chainName}`;
    else chainNames = `${chainNames},${chainName}`;
  }
  return chainNames;
};
