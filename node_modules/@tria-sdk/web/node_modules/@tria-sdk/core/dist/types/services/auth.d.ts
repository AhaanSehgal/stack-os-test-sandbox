import { DeleteAccount, Login, PasswordVerifyArgs, PinAndPasswordVerifyArgs, PinVerifyArgs, Register, UpdatePasswordArgs, UpdatePinArgs } from "../types";
import BackendService from "./index";
declare class AuthService extends BackendService {
    constructor(apiUrl: string);
    login({ triaName, password, pin }: Login): Promise<any>;
    logout(): Promise<any>;
    unauthenticated(): Promise<any>;
    deleteAccount({ password, pin }: DeleteAccount): Promise<any>;
    register({ username, usernameExtension, password, passwordVerify, pin }: Register): Promise<any>;
    updatePassword({ newPassword, oldPassword, pin, verifyNewPassword }: UpdatePasswordArgs): Promise<any>;
    checkUsername({ usernameWithExtension }: {
        usernameWithExtension: string;
    }): Promise<unknown>;
    refreshToken(): Promise<any>;
    passwordVerify({ password }: PasswordVerifyArgs): Promise<any>;
    pinVerify({ pin }: PinVerifyArgs): Promise<any>;
    pinAndPasswordVerify({ triaName, password, pin }: PinAndPasswordVerifyArgs): Promise<any>;
    updatePin({ newPin, oldPin, password, verifyNewPin }: UpdatePinArgs): Promise<any>;
    initiateOTP({ input }: {
        input: string;
    }): Promise<any>;
    verifyOTP({ otp, session, input }: {
        input: string;
        session: string;
        otp: string;
    }): Promise<any>;
    getSession(): Promise<any>;
    getKey(accessToken?: string): Promise<any>;
    saveOauthUser({ userOauthId, isPasswordRequired, password, platform, accessToken, triaNameHash }: {
        userOauthId: string;
        isPasswordRequired: boolean;
        password: string;
        platform: string;
        accessToken: string;
        triaNameHash: string;
    }): Promise<any>;
    checkLinkEmailExists({ email }: {
        email: string;
    }): Promise<any>;
    initiateEmailLinkAuth({ email, password, origin }: {
        email: string;
        password: string;
        origin?: string;
    }): Promise<any>;
    emailLinkVerification({ email, code }: {
        email: string;
        code: string;
    }): Promise<any>;
}
export default AuthService;
