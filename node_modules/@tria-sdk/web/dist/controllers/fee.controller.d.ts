import { Multichain } from '../utils/helper/multichain';
import { CheckTokenAllowanceResponse, SwapTargetResponse, FeeResponse, TriaArgs, Send, Swap, NFTDetails, ContractDetails, Approve, ENV } from '../types';
import WalletService from '../services/wallet';
export declare class FeeController extends Multichain {
    apiUrl: string;
    walletService: WalletService;
    selectedChainName: string | undefined;
    environment: ENV;
    /**
     * Constructs a new instance of the Tria class.
     * @param apiUrl - The URL of the API.
     */
    constructor({ baseUrl, walletType, selectedChainName, environment }: TriaArgs);
    /**
     * @param triaName triaName
     * @param chainName chainName
     */
    resolveTriaName(triaName: string, chainName: string): Promise<string>;
    checkTokenAllowance(triaName: string, chainName: string, payload: Approve): Promise<CheckTokenAllowanceResponse>;
    getSendFee(chainName: string, payload: Send): Promise<FeeResponse>;
    getApproveFee(triaName: string, chainName: string, payload: Approve): Promise<FeeResponse>;
    getCallContractFee(triaName: string, chainName: string, contractDetails: ContractDetails): Promise<FeeResponse>;
    getSwapTargets(chainName: string, fromTokenAddress: string | null, listLength: number, query?: string): Promise<SwapTargetResponse[]>;
    getSwapFee(chainName: string, payload: Swap): Promise<FeeResponse>;
    getSendNFTFee(chainName: string, fromTriaName: string, recipientTriaName: string, nftDetails: NFTDetails): Promise<FeeResponse>;
}
