type IframeArgs = {
    parentUrl: string;
    walletUrl: string;
};
export declare class IframeController {
    walletUrl: string;
    parentUrl: string;
    constructor({ parentUrl, walletUrl }: IframeArgs);
    handleMessageFromIframe: (event: any, type: string) => any;
    triaLogin(name: string, password: string): {
        iframeUrl: string;
    };
}
export {};
