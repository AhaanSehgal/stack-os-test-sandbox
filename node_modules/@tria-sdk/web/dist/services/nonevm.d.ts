import { Send, Swap } from "../types";
import BackendService from "./index";
declare class nonEVMService extends BackendService {
    constructor(apiUrl: string);
    send(shard1: string, chainName: string, payload: Send): Promise<any>;
    swap(shard1: string, chainName: string, payload: Swap): Promise<any>;
}
export default nonEVMService;
