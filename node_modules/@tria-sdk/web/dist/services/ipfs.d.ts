import BackendService from "./index";
declare class IPFSService extends BackendService {
    constructor(apiUrl: string);
    getKeyLinkShards(tID: string, SHA256ShadowHash: string): Promise<any>;
    resolveName(ipfsName: string): Promise<any>;
    fetchIPFSData(cid: string): Promise<any>;
}
export default IPFSService;
