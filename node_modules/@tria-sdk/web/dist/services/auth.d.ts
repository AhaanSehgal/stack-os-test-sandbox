import BackendService from "./index";
declare class AuthService extends BackendService {
    constructor(apiUrl: string);
    initiateOTP({ input }: {
        input: string;
    }): Promise<any>;
    verifyOTP({ otp, session, input }: {
        input: string;
        session: string;
        otp: string;
    }): Promise<any>;
    getSession(): Promise<any>;
    getUserSession(): Promise<any>;
    getKey(): Promise<any>;
}
export default AuthService;
