import { RemoveAddressArgs, UpdateSubNameArgs, ResolveTriaName, AddNonEvmShards, ENV } from '../types';
import BackendService from './index';
declare class WalletService extends BackendService {
    constructor(apiUrl: string);
    add(password: string, pin: string, chainName: string): Promise<any>;
    updateSubName({ newSubName, oldSubName, password, pin }: UpdateSubNameArgs): Promise<any>;
    removeSubname({ subname, password, pin }: RemoveAddressArgs): Promise<any>;
    resolveTriaName({ chainName, lookUpTriaName }: ResolveTriaName, environment?: ENV): Promise<{
        address: string;
    }>;
    addNonEvmShards({ accountKeys }: AddNonEvmShards): Promise<any>;
}
export default WalletService;
