import BackendService from './index';
import { ShardStorageArgs, CreateDIDArgs } from '../types';
declare class DIDService extends BackendService {
    constructor(apiUrl: string);
    createDID({ did, evmChainData, nonEvmChainsData }: CreateDIDArgs): Promise<unknown>;
    checkDID(did: string): Promise<unknown>;
    shardStorage({ hash, link }: ShardStorageArgs): Promise<unknown>;
    shardExists(hash: string): Promise<unknown>;
}
export default DIDService;
