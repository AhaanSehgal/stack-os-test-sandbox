import { type PublicClient, type WalletClient } from '@wagmi/core';
import { providers } from 'ethers';
export declare function publicClientToProvider(publicClient: PublicClient): providers.FallbackProvider | providers.JsonRpcProvider;
/** Action to convert a viem Public Client to an ethers.js Provider. */
export declare function getEthersProvider({ chainId }?: {
    chainId?: number;
}): providers.FallbackProvider | providers.JsonRpcProvider;
export declare function walletClientToSigner(walletClient: WalletClient): providers.JsonRpcSigner;
/** Action to convert a viem Wallet Client to an ethers.js Signer. */
export declare function getEthersSigner({ chainId }?: {
    chainId?: number;
}): Promise<providers.JsonRpcSigner | undefined>;
