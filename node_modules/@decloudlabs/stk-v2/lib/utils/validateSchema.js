"use strict";
// import Joi from "joi";
// const ipfsAppDataSchema = Joi.object({
// 	appName: Joi.string().required(),
// 	nftID: Joi.string().required(),
// 	namespace: Joi.string().required(),
// 	hostUrl: Joi.string().required(),
// 	persistenceEnabled: Joi.boolean().required(),
// 	whitelistedIps: Joi.array().items(Joi.string()).required(),
// 	networkId: Joi.string(),
// 	image: Joi.object({
// 		repository: Joi.string().required(),
// 		tag: Joi.string().required(),
// 	}).required(),
// 	path: Joi.string(),
// 	replicaCount: Joi.number(),
// 	statefulSet: Joi.boolean(),
// 	args: Joi.array().items(Joi.string()),
// 	commands: Joi.array().items(Joi.string()),
// 	envVariables: Joi.array().items(
// 		Joi.object({
// 			name: Joi.string().required(),
// 			value: Joi.string().required(),
// 		})
// 	),
// 	storageType: Joi.string().valid("standard", "ssd-sc"),
// 	storageSize: Joi.string(),
// 	volumeMounts: Joi.array().items(
// 		Joi.object({
// 			mountPath: Joi.string().required(),
// 			name: Joi.string().required(),
// 		})
// 	),
// 	resourceLimits: Joi.object({
// 		memory: Joi.string().required(),
// 		cpu: Joi.string().required(),
// 		disk: Joi.string(),
// 	}),
// 	resourceRequests: Joi.object({
// 		memory: Joi.string().required(),
// 		cpu: Joi.string().required(),
// 		disk: Joi.string(),
// 	}),
// 	status: Joi.string(),
// 	privateImage: Joi.boolean().required(),
// 	privateImageRegistry: Joi.string(),
// 	privateImageUsername: Joi.string(),
// 	privateImagePassword: Joi.string(),
// 	enableCertKey: Joi.string(),
// 	certificateValue: Joi.string(),
// 	keyValue: Joi.string(),
// 	advanceOptions: Joi.boolean().required(),
// 	isNoDeployment: Joi.boolean(),
// 	containerPort: Joi.array()
// 		.items(
// 			Joi.object({
// 				port: Joi.string().required(),
// 				protocol: Joi.string().required(),
// 			})
// 		)
// 		.required(),
// 	servicePort: Joi.array()
// 		.items(
// 			Joi.object({
// 				port: Joi.string().required(),
// 				protocol: Joi.string().required(),
// 			})
// 		)
// 		.required(),
// });
// const webttySchema = Joi.object({
// 	namespace: Joi.string().required(),
// 	username: Joi.string().required(),
// 	password: Joi.string().required(),
// 	cpu: Joi.string(),
// 	memory: Joi.string(),
// 	networkId: Joi.string().required(),
// 	nftID: Joi.string().required(),
// });
// const isValidIpfsAppData = (payload: any) => {
// 	try {
// 		payload = {
// 			...payload,
// 			containerPort: JSON.parse(payload.containerPort),
// 			servicePort: JSON.parse(payload.servicePort),
// 		};
// 		const { error } = ipfsAppDataSchema.validate(payload);
// 		if (error) throw error;
// 		else return { status: true };
// 	} catch (error) {
// 		return { status: false, message: error.message };
// 	}
// };
// const isValidWebttyData = (payload: any) => {
// 	try {
// 		const { error } = webttySchema.validate(payload);
// 		if (error) throw error;
// 		else return { status: true };
// 	} catch (error) {
// 		return { status: false, message: error.message };
// 	}
// };
// export { ipfsAppDataSchema, isValidIpfsAppData, isValidWebttyData };
