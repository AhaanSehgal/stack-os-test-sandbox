import { ContractAddresses, ETHAddress, EtherContracts } from "../types/types";
import { ethers, Signer } from "ethers";
export declare const init: (provider: ethers.providers.Provider, signer: Signer, addresses: ContractAddresses, walletAddress: ETHAddress) => EtherContracts;
