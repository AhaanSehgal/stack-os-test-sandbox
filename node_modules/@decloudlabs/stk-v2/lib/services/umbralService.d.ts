import { Capsule, PublicKey, SecretKey } from "@nucypher/umbral-pre";
import { CipherText, VerifiedCFrag } from "../types/types";
import { AsyncReturnType } from "../utils/utils";
export default class UmbralService {
    umbral: AsyncReturnType<typeof this.importUmbral> | null;
    constructor();
    getUmbral(): typeof import("@nucypher/umbral-pre");
    importUmbral(): Promise<typeof import("@nucypher/umbral-pre")>;
    initAppCrypto(): Promise<void>;
    convertToUmbralPublicKey(publicKeyBytes: number[]): PublicKey;
    convertToUmbralSecretKey(secretKeyBytes: number[]): SecretKey;
    convertToUmbralCapsule(capsuleBytes: number[]): Capsule;
    convertToUmbralCipher(cipherBytes: number[]): CipherText;
    convertToUmbralCFrag(cFragBytes: number[]): VerifiedCFrag;
}
